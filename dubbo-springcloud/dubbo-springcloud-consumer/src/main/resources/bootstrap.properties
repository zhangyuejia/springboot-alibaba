spring.application.name=dubbo-springclod-consumer
spring.cloud.nacos.discovery.server-addr=${nacos.addr}

server.port=${provider.port}
## 上报到注册中心的的宿主机ip地址，根据网络情况填写外网或内网IP
spring.cloud.nacos.discovery.ip=${consumer.ip}
## 上报到注册中心的的宿主机端口
spring.cloud.nacos.discovery.port=${consumer.port}
